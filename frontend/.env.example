# Where the Go API lives. Used by the Next.js server (build + contact proxy).
API_BASE_URL=http://localhost:8080

# Public site URL — used for canonicals, sitemap, OG, JSON-LD.
NEXT_PUBLIC_SITE_URL=https://thurakkal.com
